#!/usr/bin/env sh

if ! gem list foreman -i --silent; then
  echo "Installing foreman..."
  gem install foreman
fi

# Default to port 3005 for the GitLab Sprint Manager.
export PORT="${PORT:-3005}"

exec foreman start -f Procfile.dev "$@"
